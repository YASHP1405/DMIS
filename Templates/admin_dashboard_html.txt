
{% extends "base.html" %}
{% block content %}
<section style="max-width:1200px;margin:50px auto;padding:40px;background:#f9f9ff;
                border:2px solid darkviolet;border-radius:20px;box-shadow:0 6px 16px rgba(0,0,0,0.1);">
    
    <!-- Dashboard Title -->
    <h2 style="color:darkviolet;text-align:center;margin-bottom:10px;font-size:30px;font-weight:700;">
        Admin Dashboard
    </h2>
    <p style="text-align:center;margin-bottom:40px;color:#555;font-size:16px;">
        Manage teachers, labs, and view reports efficiently.
    </p>

    <!-- OPTIONS GRID -->
    <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(250px,1fr));gap:25px;margin-bottom:50px;">
        
        <!-- Teacher Management -->
        <a href="{{ url_for('admin_teacher_manage') }}" class="dashboard-card">
            <span style="font-size:40px;">👩‍🏫</span>
            <span>Add / Delete Teacher</span>
        </a>

        <!-- Lab Management -->
        <a href="{{ url_for('admin_lab_manage') }}" class="dashboard-card">
            <span style="font-size:40px;">🖥</span>
            <span>Add / Delete Lab</span>
        </a>

        <!-- Teacher Status -->
        <a href="{{ url_for('admin_teacher_status') }}" class="dashboard-card">
            <span style="font-size:40px;">📊</span>
            <span>Teacher Status</span>
        </a>

        <!-- Logout -->
        <a href="{{ url_for('logout') }}" class="dashboard-card logout">
            <span style="font-size:40px;">🚪</span>
            <span>Logout</span>
        </a>
    </div>

    <!-- LAB REPORTS SECTION -->
    <h3 style="color:darkviolet;margin-bottom:25px;font-size:22px;">📑 Lab Reports</h3>
    {% if labs %}
        <div style="display:flex;flex-direction:column;gap:20px;">
            {% for lab_id, lab_data in labs.items() %}
                <div class="lab-card">
                    <button class="lab-header" onclick="toggleLab('lab{{ loop.index }}')">
                        <span>Lab {{ lab_id }} - {{ lab_data['name'] }}</span>
                        <span class="arrow">▼</span>
                    </button>

                    <div id="lab{{ loop.index }}" class="lab-content">
                        {% if lab_data['reports'] %}
                            <ul style="list-style:none;padding:0;margin:0;display:flex;flex-direction:column;gap:12px;">
                                {% for time, report in lab_data['reports'].items() %}
                                    <li class="report-item">
                                        <strong>{{ time }}</strong><br>
                                        🖥 Monitors: {{ report['Monitors'] }} | 
                                        ⚙ CPU: {{ report['CPU'] }} | 
                                        🖱 Mouse: {{ report['Mouse'] }} | 
                                        ⌨ Keyboard: {{ report['Keyboard'] }} | 
                                        🔌 Switches: {{ report['Switches'] }}
                                    </li>
                                {% endfor %}
                            </ul>
                        {% else %}
                            <p style="color:#777;">No reports submitted yet.</p>
                        {% endif %}
                    </div>
                </div>
            {% endfor %}
        </div>
    {% else %}
        <p style="text-align:center;color:#555;">No labs found in database.</p>
    {% endif %}
</section>

<!-- Styles -->
<style>
    .dashboard-card {
        display:flex;flex-direction:column;align-items:center;justify-content:center;
        background:darkviolet;color:white;padding:30px;border-radius:14px;
        text-decoration:none;font-size:18px;font-weight:500;gap:10px;
        transition:transform 0.3s,box-shadow 0.3s,background 0.3s;
        text-align:center;
    }
    .dashboard-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 6px 16px rgba(0,0,0,0.2);
        background:#6a0dad;
    }
    .dashboard-card.logout { background:#c82333; }
    .dashboard-card.logout:hover { background:#a71d2a; }

    .lab-card {
        border:1px solid #ddd;
        border-radius:12px;
        background:white;
        box-shadow:0 4px 10px rgba(0,0,0,0.05);
        overflow:hidden;
    }
    .lab-header {
        width:100%;
        background:darkviolet;
        color:white;
        padding:14px 20px;
        font-size:16px;
        font-weight:bold;
        text-align:left;
        border:none;
        display:flex;
        justify-content:space-between;
        align-items:center;
        cursor:pointer;
    }
    .lab-header:hover { background:#6a0dad; }
    .lab-content {
        display:none;
        padding:15px;
    }
    .arrow { transition: transform 0.3s; }
    .lab-header.active .arrow { transform: rotate(180deg); }

    .report-item {
        padding:12px;
        background:#f9f9ff;
        border:1px solid #ccc;
        border-radius:8px;
        font-size:15px;
        color:#333;
        transition:background 0.2s;
    }
    .report-item:hover { background:#f1e6ff; }
</style>

<!-- Script for Toggle -->
<script>
    function toggleLab(id) {
        const content = document.getElementById(id);
        const header = content.previousElementSibling;

        if (content.style.display === "block") {
            content.style.display = "none";
            header.classList.remove("active");
        } else {
            content.style.display = "block";
            header.classList.add("active");
        }
    }
</script>
{% endblock %}
